# 🎨 Visual Diagram: How the POM Tests Work

## The Complete Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        TEST FILE: pom.spec.ts                               │
│                                                                              │
│ ┌──────────────────────────────────────────────────────────────────────┐   │
│ │ TEST 1: User Registration                                            │   │
│ │                                                                       │   │
│ │  test('User should register', async ({ page }) => {                 │   │
│ │      const registerPage = new RegisterPage(page) ──────────┐        │   │
│ │      await registerPage.navigateToRegister()              │        │   │
│ │      await registerPage.enterFirstName('John')            │ Uses   │   │
│ │      await registerPage.enterLastName('Doe')              │ Page   │   │
│ │      await registerPage.enterEmail(uniqueEmail)           │ Object │   │
│ │      await registerPage.enterPassword('...')              │        │   │
│ │      await registerPage.enterConfirmPassword('...')       │        │   │
│ │      await registerPage.clickCreateAccountButton()        │        │   │
│ │      expect(registerPage.getCurrentURL()).not.toContain() ┘        │   │
│ │  })                                                                  │   │
│ └──────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│ ┌──────────────────────────────────────────────────────────────────────┐   │
│ │ TEST 2: Home Navigation                                              │   │
│ │                                                                       │   │
│ │  test('User should navigate', async ({ page }) => {                 │   │
│ │      const homePage = new HomePage(page) ─────────────────┐        │   │
│ │      const jobsPage = new JobsPage(page)                 │ Uses    │   │
│ │      await homePage.navigateToHome()                     │ Page    │   │
│ │      const count = await homePage.getJobCardsCount()     │ Objects │   │
│ │      await homePage.clickJobsLink()                      │         │   │
│ │      expect(jobsPage.isJobsPageDisplayed()).toBeTruthy() ┘         │   │
│ │  })                                                                  │   │
│ └──────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│ ┌──────────────────────────────────────────────────────────────────────┐   │
│ │ TEST 3: Job Search                                                   │   │
│ │                                                                       │   │
│ │  test('User should search jobs', async ({ page }) => {              │   │
│ │      const jobsPage = new JobsPage(page) ──────────────────┐        │   │
│ │      await jobsPage.navigateToJobs()                      │ Uses   │   │
│ │      await jobsPage.searchForJobs('Developer')            │ Page   │   │
│ │      const count = await jobsPage.getJobItemsCount()      │ Object │   │
│ │      expect(jobsPage.verifyURLContains('/jobs')).toBe()   ┘        │   │
│ │  })                                                                  │   │
│ └──────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│ ┌──────────────────────────────────────────────────────────────────────┐   │
│ │ TEST 4: Login Page Elements                                          │   │
│ │                                                                       │   │
│ │  test('Login page elements visible', async ({ page }) => {          │   │
│ │      const loginPage = new LoginPage(page) ───────────────┐         │   │
│ │      await loginPage.navigateToLogin()                   │ Uses    │   │
│ │      expect(loginPage.isEmailInputVisible()).toBeTruthy() │ Page   │   │
│ │      expect(loginPage.isPasswordInputVisible()).toBeTruthy() │ Object│   │
│ │  })                                                        ┘         │   │
│ └──────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
           ↑                          ↑                      ↑
           │                          │                      │
           └──────────────────────────┴──────────────────────┘
                      All use methods from...

┌─────────────────────────────────────────────────────────────────────────────┐
│                    PAGE OBJECTS: tests/pages/                               │
│                                                                              │
│ ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│ │ LoginPage    │  │RegisterPage  │  │ HomePage     │  │ JobsPage     │    │
│ ├──────────────┤  ├──────────────┤  ├──────────────┤  ├──────────────┤    │
│ │ Properties:  │  │ Properties:  │  │ Properties:  │  │ Properties:  │    │
│ │ • emailIn    │  │ • firstNameIn│  │ • jobsLink   │  │ • searchIn   │    │
│ │ • passwordIn │  │ • emailIn    │  │ • profileLk  │  │ • filterSelect
│ │              │  │ • passwordIn │  │ • logoutBtn  │  │ • jobItems   │    │
│ │ Methods:     │  │              │  │              │  │              │    │
│ │ • navigate() │  │ Methods:     │  │ Methods:     │  │ Methods:     │    │
│ │ • enter()    │  │ • navigate() │  │ • navigate() │  │ • navigate() │    │
│ │ • login()    │  │ • enter()    │  │ • click()    │  │ • search()   │    │
│ │ • verify()   │  │ • register() │  │ • verify()   │  │ • filter()   │    │
│ │              │  │ • verify()   │  │ • verify()   │  │ • click()    │    │
│ └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘    │
│        ↑                  ↑                  ↑                  ↑            │
│        │                  │                  │                  │            │
│        └──────────────────┴──────────────────┴──────────────────┘            │
│                          All extend...                                       │
└─────────────────────────────────────────────────────────────────────────────┘
                                    ↑
                                    │
┌─────────────────────────────────────────────────────────────────────────────┐
│                    BASEPAGE: Foundation for all                             │
│                                                                              │
│  export class BasePage {                                                    │
│      readonly page: Page                                                    │
│                                                                              │
│      // Navigation                                                          │
│      goto(path) → page.goto()                                               │
│      getCurrentURL() → page.url()                                           │
│      getPageTitle() → page.title()                                          │
│                                                                              │
│      // Interactions                                                        │
│      fillInput(selector, text) → page.locator().fill()                      │
│      click(selector) → page.locator().click()                               │
│      getText(selector) → page.locator().textContent()                       │
│                                                                              │
│      // Waits                                                               │
│      waitForPageLoad() → page.waitForLoadState('networkidle')               │
│      waitForElementVisible(sel) → page.locator().waitFor()                  │
│                                                                              │
│      // Verification                                                        │
│      isElementVisible(selector) → page.locator().isVisible()                │
│      verifyURLContains(text) → page.url().includes()                        │
│  }                                                                           │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
                                    ↑
                                    │
┌─────────────────────────────────────────────────────────────────────────────┐
│              PLAYWRIGHT: The browser automation library                     │
│                                                                              │
│  • page.goto(url) - Navigate to URL                                         │
│  • page.locator(selector) - Find elements                                   │
│  • page.waitForLoadState() - Wait for page                                  │
│  • etc...                                                                   │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## Test Execution Flow - Registration Example

```
┌─────────────────────────────────────────────────────────────────┐
│ TEST STARTS                                                     │
│                                                                 │
│ test('User should register', async ({ page }) => {              │
│     const registerPage = new RegisterPage(page)                 │
│ })                                                              │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ CREATE REGISTERPAGE INSTANCE                                    │
│                                                                 │
│ new RegisterPage(page)                                          │
│   • Inherits from BasePage                                      │
│   • Has all base methods                                        │
│   • Has all register-specific methods                           │
│   • Ready to use!                                               │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ CALL: registerPage.navigateToRegister()                         │
│                                                                 │
│ Inside RegisterPage:                                            │
│   async navigateToRegister() {                                  │
│       await this.goto('/register')     ← BasePage method        │
│       await this.waitForPageLoad()     ← BasePage method        │
│   }                                                             │
│                                                                 │
│ Result: Browser navigates to /register page                     │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ VERIFY: isRegisterPageDisplayed()                               │
│                                                                 │
│ Inside RegisterPage:                                            │
│   async isRegisterPageDisplayed() {                             │
│       return await this.isElementVisible(this.pageTitle)        │
│   }                                                             │
│   // this.pageTitle = 'h1:has-text("Create Account")'           │
│                                                                 │
│ Inside BasePage:                                                │
│   async isElementVisible(selector) {                            │
│       return page.locator(selector).isVisible()                 │
│   }                                                             │
│                                                                 │
│ Result: true if page shows, false otherwise                     │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ CALL: registerPage.enterFirstName('John')                       │
│                                                                 │
│ Inside RegisterPage:                                            │
│   async enterFirstName(firstName: string) {                     │
│       await this.fillInput(this.firstNameInput, firstName)      │
│   }                                                             │
│   // this.firstNameInput = 'input[name="firstName"]'            │
│                                                                 │
│ Inside BasePage:                                                │
│   async fillInput(selector, text) {                             │
│       await page.locator(selector).fill(text)                   │
│   }                                                             │
│                                                                 │
│ In Browser: First name field fills with 'John'                  │
└─────────────────────────────────────────────────────────────────┘
                            ↓
          (Repeat for each field: lastName, email, etc.)
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ CALL: registerPage.clickCreateAccountButton()                   │
│                                                                 │
│ Inside RegisterPage:                                            │
│   async clickCreateAccountButton() {                            │
│       await this.click(this.createAccountButton)                │
│       await this.waitForPageLoad()                              │
│   }                                                             │
│   // this.createAccountButton = 'button:has-text("Create...")'  │
│                                                                 │
│ Inside BasePage:                                                │
│   async click(selector) {                                       │
│       await page.locator(selector).click()                      │
│   }                                                             │
│                                                                 │
│ In Browser: Button clicks, form submits                         │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ VERIFY: getCurrentURL() does not contain '/register'            │
│                                                                 │
│ Inside BasePage:                                                │
│   getCurrentURL() {                                             │
│       return page.url()                                         │
│   }                                                             │
│                                                                 │
│ In Test:                                                        │
│   const currentUrl = registerPage.getCurrentURL()               │
│   expect(currentUrl).not.toContain('/register')                 │
│                                                                 │
│ Result: ✅ PASS if redirected to /, /home, or other page        │
│         ❌ FAIL if still on /register                            │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ TEST COMPLETE                                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## Method Call Stack - Single Action

```
Test Code:
    await registerPage.enterFirstName('John')
                ↓
RegisterPage.enterFirstName():
    await this.fillInput(this.firstNameInput, 'John')
                ↓
BasePage.fillInput():
    await this.page.locator(selector).fill(text)
                ↓
Playwright:
    Find element 'input[name="firstName"]'
    Set its value to 'John'
                ↓
Browser:
    ✅ Input field now contains 'John'
```

---

## How It All Connects

```
┌─────────────────────────────┐
│   Test Layer                │
│   (pom.spec.ts)            │
│                             │
│ Tests call:                 │
│ • registerPage.register()   │
│ • homePage.navigate()       │
│ • jobsPage.search()         │
└──────────────┬──────────────┘
               │ Uses
               ↓
┌─────────────────────────────┐
│   Page Object Layer         │
│   (tests/pages/*.ts)       │
│                             │
│ Page objects:               │
│ • RegisterPage              │
│ • HomePage                  │
│ • JobsPage                  │
│ • LoginPage                 │
│ • BasePage (inherited)      │
└──────────────┬──────────────┘
               │ Extends & Uses
               ↓
┌─────────────────────────────┐
│   BasePage Layer            │
│   (tests/pages/BasePage)   │
│                             │
│ Common methods:             │
│ • goto()                    │
│ • fillInput()               │
│ • click()                   │
│ • waitForPageLoad()         │
│ • isElementVisible()        │
└──────────────┬──────────────┘
               │ Uses
               ↓
┌─────────────────────────────┐
│   Playwright Library        │
│                             │
│ Browser automation:         │
│ • page.goto()               │
│ • page.locator()            │
│ • page.fill()               │
│ • page.click()              │
│ • page.waitForLoadState()   │
└──────────────┬──────────────┘
               │ Controls
               ↓
┌─────────────────────────────┐
│   Browser                   │
│                             │
│ Actual user actions:        │
│ ✓ Navigate                  │
│ ✓ Fill forms                │
│ ✓ Click buttons             │
│ ✓ Read content              │
│ ✓ Verify elements           │
└─────────────────────────────┘
```

---

## 🎯 Why This is Professional POM

```
✅ SEPARATION OF CONCERNS
   Test code → What to test
   Page objects → How to interact
   Selectors → Where are elements

✅ SINGLE RESPONSIBILITY
   Tests → Define user workflows
   Page objects → Define page interactions
   BasePage → Define common actions

✅ REUSABILITY
   RegisterPage methods used by:
   • pom.spec.ts (new)
   • workflows.spec.ts (refactored)
   • Any future tests

✅ MAINTAINABILITY
   Change selector → Update ONE file
   All tests automatically fixed

✅ READABILITY
   Test reads like user story
   Easy to understand workflow
   No technical selector details
```

This is exactly how professional Playwright tests are structured! 🎉
