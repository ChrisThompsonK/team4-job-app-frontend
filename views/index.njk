{% extends "layout.njk" %}

{% block title %}Kainos Job Portal - Find Your Next Career Opportunity{% endblock %}

{% block content %}
    <!-- Hero Section with Custom Background -->
    <div class="hero min-h-96 relative overflow-hidden">
        <!-- Hero Background Image (commented out) -->
        <div class="absolute inset-0 w-full h-full">
            <img src="/images/kainos-hero-bg.jpg" 
                 alt="Kainos team members with motivational text about growth and opportunities" 
                 class="w-full h-full object-cover" />
        </div> 
        
        <!-- Hero Content -->
        <div class="hero-content text-center relative z-10">
            <div class="max-w-2xl">
                <div class="flex justify-center mb-8">
                    <div class=" p-2 rounded-lg w-48 h-24 md:w-64 md:h-32 pl-4">
                        <img src="/images/Kainos-logo.png" alt="Kainos Logo" class="w-full h-full object-contain transform scale-300" loading="lazy">
                    </div>
                </div>
                <h1 class="text-4xl md:text-5xl font-bold mb-6 text-white drop-shadow-lg">{{ title }}</h1>
                <p class="text-xl text-white mb-8 leading-relaxed drop-shadow-md">{{ message }}</p>
                <a href="/jobs" class="btn border-none btn-lg rounded-full shadow-lg hover:shadow-xl transform hover:-translate-y-1 transition-all duration-200 px-12 py-4 text-lg font-semibold !text-white" style="background-color: rgb(52, 86, 126);"
                    <span class="mr-2">🔍</span>
                    Explore Jobs
                </a>
            </div>
        </div>
    </div>

    <!-- Success Messages -->
    {% if successDisplay.show %}
        <div class="container mx-auto px-6 py-8 relative z-20">
            <div class="{{ successDisplay.containerClass }} shadow-lg">
                <div class="flex items-center justify-center">
                    <i data-lucide="{{ successDisplay.iconClass }}" class="h-5 w-5 mr-2"></i>
                    {{ successDisplay.message }}
                </div>
            </div>
        </div>
    {% endif %}

    <!-- Latest Jobs Section -->
    <div class="container mx-auto px-4 py-16">
        <h2 class="text-3xl font-bold text-center mb-12">Latest Job Opportunities</h2>
        {% if latestJobs and latestJobs.length > 0 %}
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                {% for job in latestJobs %}
                    <!-- Job Card -->
                    <div class="card bg-base-100 shadow-xl hover:shadow-2xl transition-all duration-300">
                        <div class="card-body p-6">
                            <h3 class="card-title text-xl font-bold text-gray-800 mb-4">{{ job.name }}</h3>
                            <div class="space-y-3 mb-6">
                                <div class="flex items-center text-gray-600">
                                    <i data-lucide="map-pin" class="h-5 w-5 mr-3 text-blue-600" aria-hidden="true"></i>
                                    <span class="text-sm">{{ job.location }}</span>
                                </div>
                                <div class="flex items-center text-gray-600">
                                    <i data-lucide="building" class="h-5 w-5 mr-3 text-purple-600" aria-hidden="true"></i>
                                    <span class="text-sm">{{ job.capability }}</span>
                                </div>
                                <div class="flex items-center text-gray-600">
                                    <i data-lucide="badge" class="h-5 w-5 mr-3 text-green-600" aria-hidden="true"></i>
                                    <span class="text-sm">{{ job.band }}</span>
                                </div>
                                <div class="flex items-center text-red-600 font-medium">
                                    <i data-lucide="calendar" class="h-5 w-5 mr-3" aria-hidden="true"></i>
                                    <span class="text-sm">Closes: {{ job.closingDate }}</span>
                                </div>
                                <div class="flex items-center">
                                    {% if job.status == 'open' %}
                                        <i data-lucide="circle-check" class="h-5 w-5 mr-3 text-green-600" aria-hidden="true"></i>
                                        <span class="text-sm text-green-600 font-medium badge badge-success badge-sm">Open</span>
                                    {% else %}
                                        <i data-lucide="circle-x" class="h-5 w-5 mr-3 text-red-600" aria-hidden="true"></i>
                                        <span class="text-sm text-red-600 font-medium badge badge-error badge-sm">Closed</span>
                                    {% endif %}
                                </div>
                                <div class="flex items-center text-gray-600">
                                    <i data-lucide="users" class="h-5 w-5 mr-3 text-orange-600" aria-hidden="true"></i>
                                    <span class="text-sm">{{ job.numberOfOpenPositions }} position{% if job.numberOfOpenPositions != 1 %}s{% endif %}</span>
                                </div>
                            </div>
                            <div class="card-actions justify-center">
                                {% if job.status == 'open' and job.numberOfOpenPositions > 0 %}
                                    {% if isAuthenticated and currentUser and currentUser.role == "admin" %}
                                        <a href="/jobs/{{ job.id }}/applications" class="btn w-full rounded-full shadow-md hover:shadow-lg transform hover:-translate-y-0.5 transition-all duration-200 text-white" style="background-color: rgb(52, 86, 126);">
                                            Manage Applications
                                        </a>
                                    {% elif isAuthenticated %}
                                        <a href="/jobs/{{ job.id }}/apply" class="btn w-full rounded-full shadow-md hover:shadow-lg transform hover:-translate-y-0.5 transition-all duration-200 text-white" style="background-color: rgb(52, 86, 126);">
                                            Apply Now
                                        </a>
                                    {% else %}
                                        <a href="/login?redirectTo=/jobs/{{ job.id }}/apply" class="btn w-full rounded-full shadow-md hover:shadow-lg transform hover:-translate-y-0.5 transition-all duration-200 text-white" style="background-color: rgb(52, 86, 126);">
                                            Login to Apply
                                        </a>
                                    {% endif %}
                                {% elif job.status == 'open' and job.numberOfOpenPositions == 0 %}
                                    <button class="btn btn-disabled w-full cursor-not-allowed" disabled aria-label="No positions available">
                                        No Positions
                                    </button>
                                {% else %}
                                    <button class="btn btn-disabled w-full cursor-not-allowed" disabled aria-label="Job closed">
                                        Closed
                                    </button>
                                {% endif %}
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
            <div class="text-center mt-12">
                <a href="/jobs" class="btn btn-lg text-white border-none rounded-full shadow-lg hover:shadow-xl transform hover:-translate-y-1 transition-all duration-200 px-8" style="background: linear-gradient(to right, rgb(52, 86, 126), #61A943);">
                    <span class="text-white">View All Jobs</span>
                    <i data-lucide="arrow-right" class="h-5 w-5 ml-2 text-white" aria-hidden="true"></i>
                </a>
            </div>
        {% else %}
            <div class="text-center py-16">
                <i data-lucide="briefcase" class="h-16 w-16 text-gray-400 mx-auto mb-4" aria-hidden="true"></i>
                <h3 class="text-xl font-semibold text-gray-700 mb-2">No Jobs Available</h3>
                <p class="text-gray-600">Check back soon for new career opportunities at Kainos!</p>
                <a href="/jobs" class="btn btn-outline mt-4 text-white border-2" style="background-color: rgb(52, 86, 126); border-color: rgb(52, 86, 126);">
                    Refresh Job Listings
                </a>
            </div>
        {% endif %}
    </div>

{% endblock %}
