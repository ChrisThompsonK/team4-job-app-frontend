<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body class="bg-gray-100 font-sans">
    <div class="max-w-4xl mx-auto p-8">
        <div class="bg-white rounded-lg shadow-lg p-8">
            <nav class="text-center mb-8">
                <a href="/" class="text-blue-600 hover:text-blue-800 hover:underline mx-4 font-medium">Home</a>
                <a href="/jobs" class="text-blue-600 hover:text-blue-800 hover:underline mx-4 font-medium">Jobs</a>
            </nav>
            
            <h1 class="text-3xl font-bold text-gray-800 text-center mb-8">{{ title }}</h1>
            
            {% if jobs %}
                <div class="space-y-6 mt-8">
                    {% for job in jobs %}
                        <div class="bg-gray-50 p-6 rounded-lg border-l-4 border-blue-500 shadow-sm hover:shadow-md hover:-translate-y-1 transition-all duration-200">
                            <div class="text-xl font-bold text-gray-800 mb-3">{{ job.name }}</div>
                            <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
                                <div class="text-gray-600 text-sm flex items-center">
                                    <span class="mr-2">📍</span>{{ job.location }}
                                </div>
                                <div class="text-gray-600 text-sm flex items-center">
                                    <span class="mr-2">🏢</span>{{ job.capability }}
                                </div>
                                <div class="text-gray-600 text-sm flex items-center">
                                    <span class="mr-2">🎯</span>{{ job.band }}
                                </div>
                                <div class="text-red-600 text-sm font-medium flex items-center md:col-span-2">
                                    <span class="mr-2">📅</span>Closing: {{ job.closingDate }}
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                </div>
            {% else %}
                <p class="text-center text-gray-600 text-lg mt-8">No job roles available at the moment.</p>
            {% endif %}
        </div>
    </div>
    
    <!-- Include main JavaScript functionality -->
    <script src="/js/index.js"></script>
</body>
</html>
