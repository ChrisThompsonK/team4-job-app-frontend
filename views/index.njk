{% extends "layout.njk" %}

{% block title %}Kainos Job Portal - Find Your Next Career Opportunity{% endblock %}

{% block content %}
    <!-- Hero Section -->
    <div class="min-h-96 bg-gradient-to-r from-blue-50 to-purple-50 flex items-center justify-center">
        <div class="text-center px-4">
            <div class="max-w-2xl mx-auto">
                <div class="flex justify-center mb-6">
                    <div class="bg-gradient-to-r from-blue-50 to-purple-50 p-3 rounded-lg shadow-sm">
                        <img src="/images/Kainos-logo.jpg" alt="Kainos Logo" class="h-16">
                    </div>
                </div>
                <p class="text-xl text-gray-700 mb-16">{{ message }}</p>
                <a href="/jobs" class="btn bg-blue-600 hover:bg-blue-700 text-white border-none btn-lg rounded-full shadow-lg hover:shadow-xl transform hover:-translate-y-1 transition-all duration-200 px-12 py-4 text-lg font-semibold !text-white mt-8">Explore Jobs</a>
            </div>
        </div>
    </div>

    <!-- Latest Jobs Section -->
    <div class="container mx-auto px-4 py-16">
        <h2 class="text-3xl font-bold text-center mb-12">Latest Jobs</h2>
        {% if latestJobs and latestJobs.length > 0 %}
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                {% for job in latestJobs %}
                    <!-- Job Card -->
                    <div class="card bg-base-100 shadow-xl feature-card">
                        <div class="card-body p-6">
                            <h2 class="card-title text-xl font-bold text-gray-800 mb-4">{{ job.name }}</h2>
                            <div class="space-y-3 mb-6">
                                <div class="flex items-center text-gray-600">
                                    <i data-lucide="map-pin" class="h-5 w-5 mr-3 text-blue-600"></i>
                                    <span class="text-sm">{{ job.location }}</span>
                                </div>
                                <div class="flex items-center text-gray-600">
                                    <i data-lucide="building" class="h-5 w-5 mr-3 text-purple-600"></i>
                                    <span class="text-sm">{{ job.capability }}</span>
                                </div>
                                <div class="flex items-center text-gray-600">
                                    <i data-lucide="badge" class="h-5 w-5 mr-3 text-green-600"></i>
                                    <span class="text-sm">{{ job.band }}</span>
                                </div>
                                <div class="flex items-center text-red-600 font-medium">
                                    <i data-lucide="calendar" class="h-5 w-5 mr-3"></i>
                                    <span class="text-sm">Closes: {{ job.closingDate }}</span>
                                </div>
                                <div class="flex items-center">
                                    {% if job.status == 'open' %}
                                        <i data-lucide="circle-check" class="h-5 w-5 mr-3 text-green-600"></i>
                                        <span class="text-sm text-green-600 font-medium">Open</span>
                                    {% else %}
                                        <i data-lucide="circle-x" class="h-5 w-5 mr-3 text-red-600"></i>
                                        <span class="text-sm text-red-600 font-medium">Closed</span>
                                    {% endif %}
                                </div>
                                <div class="flex items-center text-gray-600">
                                    <i data-lucide="users" class="h-5 w-5 mr-3 text-orange-600"></i>
                                    <span class="text-sm">{{ job.numberOfOpenPositions }} position{% if job.numberOfOpenPositions != 1 %}s{% endif %}</span>
                                </div>
                            </div>
                            <div class="card-actions justify-center">
                                {% if job.status == 'open' and job.numberOfOpenPositions > 0 %}
                                    <a href="/jobs/{{ job.id }}" class="btn bg-blue-600 hover:bg-blue-700 text-white border-none w-full rounded-full shadow-md hover:shadow-lg transform hover:-translate-y-0.5 transition-all duration-200 inline-block text-center !text-white">Apply Now</a>
                                {% elif job.status == 'open' and job.numberOfOpenPositions == 0 %}
                                    <button class="btn bg-gray-400 text-white border-none w-full cursor-not-allowed" disabled>No Positions</button>
                                {% else %}
                                    <button class="btn bg-gray-400 text-white border-none w-full cursor-not-allowed" disabled>Closed</button>
                                {% endif %}
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
            <div class="text-center mt-8">
                <a href="/jobs" class="inline-flex items-center px-8 py-3 bg-gradient-to-r from-purple-600 to-blue-600 hover:from-purple-700 hover:to-blue-700 text-white font-semibold rounded-lg shadow-lg hover:shadow-xl transform hover:-translate-y-1 transition-all duration-200">
                    <span class="text-white">View All Jobs</span>
                    <i data-lucide="arrow-right" class="h-5 w-5 ml-2 text-white"></i>
                </a>
            </div>
        {% else %}
            <div class="text-center py-16">
                <i data-lucide="briefcase" class="h-16 w-16 text-gray-400 mx-auto mb-4"></i>
                <h3 class="text-xl font-semibold text-gray-700 mb-2">No Jobs Available</h3>
                <p class="text-gray-600">Check back soon for new career opportunities at Kainos!</p>
            </div>
        {% endif %}
    </div>
{% endblock %}
