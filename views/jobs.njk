<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="/common.css">
    <link rel="stylesheet" href="/jobs.css">
</head>
<body>
    <div class="container">
        <nav>
            <a href="/">Home</a>
            <a href="/jobs">Jobs</a>
        </nav>
        
        <h1>{{ title }}</h1>
        
        {% if jobs %}
            <div class="jobs-list">
                {% for job in jobs %}
                    <div class="job-item">
                        <div class="job-title">{{ job.name }}</div>
                        <div class="job-details">
                            <div class="job-location">📍 {{ job.location }}</div>
                            <div class="job-capability">🏢 {{ job.capability }}</div>
                            <div class="job-band">🎯 {{ job.band }}</div>
                            <div class="job-closing">📅 Closing: {{ job.closingDate }}</div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% else %}
            <p>No job roles available at the moment.</p>
        {% endif %}
    </div>
</body>
</html>
